import 'package:flutter/material.dart';

class ScreenInventary extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gestionar Inventario'),
      ),
      body: Center(
        child: Text('Pantalla de Inventario'),
      ),
    );
  }
}
