import 'package:flutter/material.dart';

class ScreenRepots extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ver Reportes'),
      ),
      body: Center(
        child: Text('Pantalla de Reportes'),
      ),
    );
  }
}
